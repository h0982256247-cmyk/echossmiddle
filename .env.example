# ── Rezio API ──────────────────────────────────────────────────
REZIO_STORE_UUID=your_store_uuid
REZIO_API_KEY=your_api_key
REZIO_LANG=zh-TW

# ── Echoss API ─────────────────────────────────────────────────
ECHOSS_API_URL=https://your-echoss-domain.com
ECHOSS_API_TOKEN=your_bearer_token

# ── Supabase ───────────────────────────────────────────────────
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your_service_role_key

# JWT secret 用於本地驗簽（不需要打 Supabase API）
# 位置：Supabase Dashboard → Settings → API → JWT Secret
SUPABASE_JWT_SECRET=your_jwt_secret

# ── Gmail OAuth2（寄信用）──────────────────────────────────────
GMAIL_USER=your@gmail.com
GMAIL_CLIENT_ID=your_client_id.apps.googleusercontent.com
GMAIL_CLIENT_SECRET=your_client_secret
GMAIL_REFRESH_TOKEN=your_refresh_token

# 週報收件人
REPORT_TO=recipient@email.com

# ── 安全驗證 ───────────────────────────────────────────────────
# Supabase pg_cron 觸發 /api/run 時帶的 Bearer token
CRON_SECRET=your_random_secret_string_at_least_32_chars

# CORS 允許的前端來源（留空則不加 CORS header，same-origin 情況不需要設定）
# 若前端與後端不同 domain，填入前端 URL，例如：https://yourdomain.com
ALLOWED_ORIGIN=

# ── 測試模式 ───────────────────────────────────────────────────
# true 時所有信件改寄到 REPORT_TO，不寄給消費者
TEST_MODE=false
